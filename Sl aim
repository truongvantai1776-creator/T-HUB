--==================================================
-- T HUB - INVISIBLE BODY HITBOX MODULE (WITH FOV & ESP)
--==================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Player = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local PlayerGui = Player:WaitForChild("PlayerGui")

-- Cấu hình thông số mặc định
local Settings = {
    Enabled = true,           
    AimLockFOV = 100,         
    HitboxCheckTeam = true,   
    HitboxTargetPart = "HumanoidRootPart",
    ESPEnabled = true         -- Bật/tắt tính năng ESP trong FOV
}

local OriginalHitboxSizes = {}
local DeathConnections = {}
local Highlights = {}

-- Xóa UI cũ nếu có
local OldGui = PlayerGui:FindFirstChild("THub_ExtractedPanel")
if OldGui then OldGui:Destroy() end

local OldFOV = PlayerGui:FindFirstChild("THubFOV_Extracted")
if OldFOV then OldFOV:Destroy() end

-- ================= 1. TẠO VÒNG TRÒN FOV HIỂN THỊ VĨNH VIỄN =================
local FOVGui = Instance.new("ScreenGui")
FOVGui.Name = "THubFOV_Extracted"
FOVGui.ResetOnSpawn = false
FOVGui.IgnoreGuiInset = true
FOVGui.Parent = PlayerGui

local FOVCircle = Instance.new("Frame")
FOVCircle.Name = "FOVCircle"
FOVCircle.AnchorPoint = Vector2.new(0.5, 0.5)
FOVCircle.Position = UDim2.new(0.5, 0, 0.5, 0)
FOVCircle.BackgroundTransparency = 1
FOVCircle.Parent = FOVGui

local FOVConstraint = Instance.new("UIAspectRatioConstraint")
FOVConstraint.AspectRatio = 1
FOVConstraint.Parent = FOVCircle

local FOVStroke = Instance.new("UIStroke")
FOVStroke.Color = Color3.fromRGB(255, 0, 0)
FOVStroke.Thickness = 2
FOVStroke.Parent = FOVCircle

local FOVCorner = Instance.new("UICorner")
FOVCorner.CornerRadius = UDim.new(1, 0)
FOVCorner.Parent = FOVCircle


-- ================= 2. TẠO GIAO DIỆN (UI) NÚT MỞ/ĐÓNG MENU =================
local Gui = Instance.new("ScreenGui")
Gui.Name = "THub_ExtractedPanel"
Gui.ResetOnSpawn = false
Gui.Parent = PlayerGui

local OpenBtn = Instance.new("TextButton")
OpenBtn.Size = UDim2.fromOffset(35, 35)
OpenBtn.Position = UDim2.new(0, 15, 0.5, -17)
OpenBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
OpenBtn.Text = "T"
OpenBtn.TextColor3 = Color3.new(1, 1, 1)
OpenBtn.TextSize = 16
OpenBtn.Font = Enum.Font.GothamBold
OpenBtn.BorderSizePixel = 0
OpenBtn.Parent = Gui
Instance.new("UICorner", OpenBtn).CornerRadius = UDim.new(0, 8)

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.fromOffset(220, 200) -- Mở rộng chiều cao chứa thêm nút ESP
MainFrame.Position = UDim2.new(0, 60, 0.5, -100)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 35, 50)
MainFrame.BorderSizePixel = 0
MainFrame.Visible = false
MainFrame.Parent = Gui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(0, 170, 255)
MainStroke.Thickness = 2
MainStroke.Parent = MainFrame

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 0, 25)
TitleLabel.Position = UDim2.fromOffset(0, 4)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "T HUB SETTINGS"
TitleLabel.TextColor3 = Color3.new(1, 1, 1)
TitleLabel.TextSize = 12
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Parent = MainFrame

local function CreateMenuToggle(parent, text, defaultState, callback)
    local Container = Instance.new("Frame")
    Container.Size = UDim2.new(1, -16, 0, 30)
    Container.BackgroundTransparency = 1
    Container.Parent = parent

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(1, -50, 1, 0)
    Label.BackgroundTransparency = 1
    Label.Text = text
    Label.TextColor3 = Color3.new(1, 1, 1)
    Label.TextSize = 11
    Label.Font = Enum.Font.GothamBold
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Container

    local ToggleBtn = Instance.new("TextButton")
    ToggleBtn.Size = UDim2.fromOffset(38, 20)
    ToggleBtn.AnchorPoint = Vector2.new(1, 0.5)
    ToggleBtn.Position = UDim2.new(1, 0, 0.5, 0)
    ToggleBtn.BackgroundColor3 = defaultState and Color3.fromRGB(0, 170, 255) or Color3.fromRGB(120, 120, 120)
    ToggleBtn.Text = ""
    ToggleBtn.AutoButtonColor = false
    ToggleBtn.BorderSizePixel = 0
    ToggleBtn.Parent = Container
    Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(1, 0)

    local Knob = Instance.new("Frame")
    Knob.Size = UDim2.fromOffset(16, 16)
    Knob.AnchorPoint = Vector2.new(0, 0.5)
    Knob.Position = defaultState and UDim2.new(1, -18, 0.5, 0) or UDim2.new(0, 2, 0.5, 0)
    Knob.BackgroundColor3 = Color3.new(1, 1, 1)
    Knob.BorderSizePixel = 0
    Knob.Parent = ToggleBtn
    Instance.new("UICorner", Knob).CornerRadius = UDim.new(1, 0)

    local state = defaultState
    ToggleBtn.MouseButton1Click:Connect(function()
        state = not state
        local targetColor = state and Color3.fromRGB(0, 170, 255) or Color3.fromRGB(120, 120, 120)
        local targetPos = state and UDim2.new(1, -18, 0.5, 0) or UDim2.new(0, 2, 0.5, 0)
        
        TweenService:Create(ToggleBtn, TweenInfo.new(0.2), {BackgroundColor3 = targetColor}):Play()
        TweenService:Create(Knob, TweenInfo.new(0.2), {Position = targetPos}):Play()

        callback(state)
    end)
end

-- Hàm tạo thanh trượt (Slider) tùy chỉnh FOV từ 50 đến 150
local function CreateFOVSlider(parent, text, min, max, defaultVal, callback)
    local Container = Instance.new("Frame")
    Container.Size = UDim2.new(1, -16, 0, 40)
    Container.BackgroundTransparency = 1
    Container.Parent = parent

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(1, 0, 0, 18)
    Label.BackgroundTransparency = 1
    Label.Text = text .. ": " .. tostring(defaultVal)
    Label.TextColor3 = Color3.new(1, 1, 1)
    Label.TextSize = 11
    Label.Font = Enum.Font.GothamBold
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Container

    local SliderBar = Instance.new("Frame")
    SliderBar.Size = UDim2.new(1, 0, 0, 8)
    SliderBar.Position = UDim2.new(0, 0, 0, 24)
    SliderBar.BackgroundColor3 = Color3.fromRGB(40, 60, 80)
    SliderBar.BorderSizePixel = 0
    SliderBar.Parent = Container
    Instance.new("UICorner", SliderBar).CornerRadius = UDim.new(1, 0)

    local FillBar = Instance.new("Frame")
    FillBar.Size = UDim2.new((defaultVal - min) / (max - min), 0, 1, 0)
    FillBar.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
    FillBar.BorderSizePixel = 0
    FillBar.Parent = SliderBar
    Instance.new("UICorner", FillBar).CornerRadius = UDim.new(1, 0)

    local Knob = Instance.new("Frame")
    Knob.Size = UDim2.fromOffset(14, 14)
    Knob.AnchorPoint = Vector2.new(0.5, 0.5)
    Knob.Position = UDim2.new(1, 0, 0.5, 0)
    Knob.BackgroundColor3 = Color3.new(1, 1, 1)
    Knob.BorderSizePixel = 0
    Knob.Parent = FillBar
    Instance.new("UICorner", Knob).CornerRadius = UDim.new(1, 0)

    local dragging = false

    local function UpdateInput(input)
        local pos = math.clamp((input.Position.X - SliderBar.AbsolutePosition.X) / SliderBar.AbsoluteSize.X, 0, 1)
        local val = math.floor(min + (max - min) * pos)
        FillBar.Size = UDim2.new(pos, 0, 1, 0)
        Label.Text = text .. ": " .. tostring(val)
        callback(val)
    end

    SliderBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            UpdateInput(input)
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            UpdateInput(input)
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
end

local UIList = Instance.new("UIListLayout")
UIList.HorizontalAlignment = Enum.HorizontalAlignment.Center
UIList.SortOrder = Enum.SortOrder.LayoutOrder
UIList.Padding = UDim.new(0, 4)
UIList.Parent = MainFrame
TitleLabel.LayoutOrder = 1

local mainToggleHolder = Instance.new("Frame")
mainToggleHolder.Size = UDim2.new(1, -16, 0, 30)
mainToggleHolder.BackgroundTransparency = 1
mainToggleHolder.LayoutOrder = 2
mainToggleHolder.Parent = MainFrame

CreateMenuToggle(mainToggleHolder, "Bật/Tắt Tính Năng", Settings.Enabled, function(v)
    Settings.Enabled = v
end)

local checkTeamHolder = Instance.new("Frame")
checkTeamHolder.Size = UDim2.new(1, -16, 0, 30)
checkTeamHolder.BackgroundTransparency = 1
checkTeamHolder.LayoutOrder = 3
checkTeamHolder.Parent = MainFrame

CreateMenuToggle(checkTeamHolder, "Kiểm Tra Team", Settings.HitboxCheckTeam, function(v)
    Settings.HitboxCheckTeam = v
end)

local espToggleHolder = Instance.new("Frame")
espToggleHolder.Size = UDim2.new(1, -16, 0, 30)
espToggleHolder.BackgroundTransparency = 1
espToggleHolder.LayoutOrder = 4
espToggleHolder.Parent = MainFrame

CreateMenuToggle(espToggleHolder, "Bật/Tắt ESP FOV", Settings.ESPEnabled, function(v)
    Settings.ESPEnabled = v
end)

local fovSliderHolder = Instance.new("Frame")
fovSliderHolder.Size = UDim2.new(1, -16, 0, 45)
fovSliderHolder.BackgroundTransparency = 1
fovSliderHolder.LayoutOrder = 5
fovSliderHolder.Parent = MainFrame

CreateFOVSlider(fovSliderHolder, "Kích Thước FOV", 50, 150, Settings.AimLockFOV, function(v)
    Settings.AimLockFOV = v
end)

OpenBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)


-- ================= 3. XỬ LÝ HITBOX & ESP CHO PLAYER VÀ NPC =================
local function IsTeammate(char) 
    local player = Players:GetPlayerFromCharacter(char)
    if player and Settings.HitboxCheckTeam then
        return Player.Team and player.Team == Player.Team 
    end
    return false
end

local function ResetHitboxForPart(part)
    if part and OriginalHitboxSizes[part] then
        part.Size = OriginalHitboxSizes[part]
        part.Transparency = 0
        part.CanCollide = true
        OriginalHitboxSizes[part] = nil
    end
end

local function RemoveHighlight(char)
    if Highlights[char] then
        Highlights[char]:Destroy()
        Highlights[char] = nil
    end
end

local function IsAlive(model)
    local humanoid = model:FindFirstChildOfClass("Humanoid")
    local rootPart = model:FindFirstChild("HumanoidRootPart")
    return humanoid and rootPart and humanoid.Health > 0
end

local function CheckWall(targetPart, character)
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    
    local filterList = {Player.Character}
    if character then
        table.insert(filterList, character)
    end
    raycastParams.FilterDescendantsInstances = filterList
    
    local checkPos = targetPart.Position
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    if rootPart then
        checkPos = rootPart.Position
    end

    local direction = checkPos - Camera.CFrame.Position
    local result = workspace:Raycast(Camera.CFrame.Position, direction, raycastParams)
    
    return result == nil
end

RunService.RenderStepped:Connect(function()
    local mousePos = Camera.ViewportSize / 2

    local diameter = Settings.AimLockFOV * 2
    FOVCircle.Size = UDim2.fromOffset(diameter, diameter)
    FOVCircle.Visible = Settings.Enabled

    local targets = {}
    
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= Player and p.Character then
            table.insert(targets, p.Character)
        end
    end
    
    for _, obj in ipairs(workspace:GetChildren()) do
        if obj:IsA("Model") and not Players:GetPlayerFromCharacter(obj) then
            if obj:FindFirstChildOfClass("Humanoid") and obj:FindFirstChild("HumanoidRootPart") then
                table.insert(targets, obj)
            end
        end
    end

    local activeTracked = {}

    for _, char in ipairs(targets) do
        local targetPart = char:FindFirstChild(Settings.HitboxTargetPart)
        local humanoid = char:FindFirstChildOfClass("Humanoid")
        local isAlive = IsAlive(char)
        local isTeamMatch = IsTeammate(char)

        -- Lắng nghe sự kiện chết ngay lập tức
        if humanoid and not DeathConnections[char] then
            DeathConnections[char] = humanoid.Died:Connect(function()
                if targetPart then
                    ResetHitboxForPart(targetPart)
                end
                RemoveHighlight(char)
                if DeathConnections[char] then
                    DeathConnections[char]:Disconnect()
                    DeathConnections[char] = nil
                end
            end)
        end

        if not Settings.Enabled or not isAlive or isTeamMatch or not targetPart then
            if targetPart then ResetHitboxForPart(targetPart) end
            RemoveHighlight(char)
        else
            activeTracked[char] = true
            local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
            local inFOV = false

            if onScreen then
                local distToCenter = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                if distToCenter <= Settings.AimLockFOV then
                    inFOV = true
                end
            end

            if inFOV then
                local hasNoWall = CheckWall(targetPart, char)

                -- Quản lý ESP (Highlight)
                if Settings.ESPEnabled then
                    if not Highlights[char] then
                        local hl = Instance.new("Highlight")
                        hl.Parent = char
                        hl.Adornee = char
                        hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                        hl.FillTransparency = 0.5
                        hl.OutlineTransparency = 0
                        Highlights[char] = hl
                    end
                    
                    if hasNoWall then
                        Highlights[char].FillColor = Color3.fromRGB(0, 255, 0)   -- Xanh lá cây (Không có vật cản)
                        Highlights[char].OutlineColor = Color3.fromRGB(0, 255, 0)
                    else
                        Highlights[char].FillColor = Color3.fromRGB(255, 0, 0)   -- Đỏ (Có vật cản che)
                        Highlights[char].OutlineColor = Color3.fromRGB(255, 0, 0)
                    end
                    Highlights[char].Enabled = true
                else
                    RemoveHighlight(char)
                end

                if hasNoWall then
                    -- Không có vật cản: PHÓNG TO HITBOX THÂN VÀ TÀNG HÌNH
                    if not OriginalHitboxSizes[targetPart] then
                        OriginalHitboxSizes[targetPart] = targetPart.Size
                    end
                    
                    local distance = (Camera.CFrame.Position - targetPart.Position).Magnitude
                    local fullDiameterPixel = Settings.AimLockFOV * 2
                    local calculatedSize = (fullDiameterPixel * distance) / Camera.ViewportSize.Y * 1.5
                    
                    targetPart.Size = Vector3.new(calculatedSize, calculatedSize, calculatedSize)
                    targetPart.Transparency = 1 
                    targetPart.CanCollide = false
                else
                    -- Có vật cản: TRẢ VỀ KÍCH THƯỚC BAN ĐẦU
                    ResetHitboxForPart(targetPart)
                end
            else
                ResetHitboxForPart(targetPart)
                RemoveHighlight(char)
            end
        end
    end

    -- Dọn dẹp trạng thái khi mục tiêu không còn tồn tại
    for part, _ in pairs(OriginalHitboxSizes) do
        local found = false
        for _, char in ipairs(targets) do
            if char:FindFirstChild(Settings.HitboxTargetPart) == part and IsAlive(char) then
                found = true
                break
            end
        end
        if not found then
            ResetHitboxForPart(part)
        end
    end

    for char, _ in pairs(Highlights) do
        local found = false
        for _, tChar in ipairs(targets) do
            if tChar == char and IsAlive(char) then
                found = true
                break
            end
        end
        if not found then
            RemoveHighlight(char)
        end
    end
end)
