-- ================= SCRIPT FISHING MASTER (TỐC ĐỘ CLICK CỰC NHANH & SIÊU LẶP) =================
local CoreGui = game:GetService("CoreGui")
if CoreGui:FindFirstChild("RobloxFishingMasterV12") then
    CoreGui.RobloxFishingMasterV12:Destroy()
end

local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local GuiService = game:GetService("GuiService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local Camera = workspace.CurrentCamera

-- Tạo ScreenGUI chính
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RobloxFishingMasterV12"
ScreenGui.Parent = CoreGui

-- Biến lưu thời gian Auto Sell (Mặc định 300 giây = 5 phút)
local autoSellSeconds = 300

-- Cố định kích thước vùng random: Rộng (X) = 120, Dài/Cao (Y) = 60
local boxWidthSize = 120  
local boxHeightSize = 60 

-- ================= NÚT BẬT/TẮT GIAO DIỆN (CHỮ T) =================
local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(0, 40, 0, 40)
ToggleButton.Position = UDim2.new(0, 50, 0, 100)
ToggleButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
ToggleButton.Text = "T"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 18
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Active = true
ToggleButton.Draggable = true
ToggleButton.Parent = ScreenGui

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(1, 0)
ToggleCorner.Parent = ToggleButton

local ToggleStroke = Instance.new("UIStroke")
ToggleStroke.Color = Color3.fromRGB(0, 255, 200)
ToggleStroke.Thickness = 2
ToggleStroke.Parent = ToggleButton

-- Khung chính giao diện
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 190, 0, 165)
MainFrame.Position = UDim2.new(0.5, -95, 0.2, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(100, 100, 255)
MainStroke.Thickness = 2
MainStroke.Parent = MainFrame

ToggleButton.MouseButton1Click:Connect(function()
    local targetVis = not MainFrame.Visible
    MainFrame.Visible = targetVis
end)

-- Tiêu đề
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 28)
Title.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
Title.Text = "✨ FISHING MASTER ✨"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 10
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = Title

local TitleGradient = Instance.new("UIGradient")
TitleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 100, 100)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(100, 255, 200)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(200, 100, 255))
})
TitleGradient.Parent = Title

-- Nút Đóng GUI (X)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 22, 0, 22)
CloseBtn.Position = UDim2.new(1, -25, 0, 3)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
CloseBtn.TextSize = 12
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = MainFrame

CloseBtn.MouseButton1Click:Connect(function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = 16
        LocalPlayer.Character.Humanoid.JumpPower = 50
    end
    Camera.CameraType = Enum.CameraType.Custom
    ScreenGui:Destroy()
end)

-- Nút Auto Câu + Skill
local ToggleMasterBtn = Instance.new("TextButton")
ToggleMasterBtn.Size = UDim2.new(1, -16, 0, 28)
ToggleMasterBtn.Position = UDim2.new(0, 8, 0, 34)
ToggleMasterBtn.BackgroundColor3 = Color3.fromRGB(40, 200, 90)
ToggleMasterBtn.Text = "auto câu + skill"
ToggleMasterBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleMasterBtn.TextSize = 9
ToggleMasterBtn.Font = Enum.Font.GothamBold
ToggleMasterBtn.Parent = MainFrame
Instance.new("UICorner", ToggleMasterBtn).CornerRadius = UDim.new(0, 6)

-- Nút Speed Buff
local SpeedBuffBtn = Instance.new("TextButton")
SpeedBuffBtn.Size = UDim2.new(1, -16, 0, 28)
SpeedBuffBtn.Position = UDim2.new(0, 8, 0, 66)
SpeedBuffBtn.BackgroundColor3 = Color3.fromRGB(60, 100, 200)
SpeedBuffBtn.Text = "⚡ Speed Buff: TẮT (40)"
SpeedBuffBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedBuffBtn.TextSize = 9
SpeedBuffBtn.Font = Enum.Font.GothamBold
SpeedBuffBtn.Parent = MainFrame
Instance.new("UICorner", SpeedBuffBtn).CornerRadius = UDim.new(0, 6)

-- Nút Auto Sell
local AutoSellBtn = Instance.new("TextButton")
AutoSellBtn.Size = UDim2.new(1, -56, 0, 28)
AutoSellBtn.Position = UDim2.new(0, 8, 0, 98)
AutoSellBtn.BackgroundColor3 = Color3.fromRGB(180, 100, 40)
AutoSellBtn.Text = "📦 Auto Sell: TẮT"
AutoSellBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AutoSellBtn.TextSize = 9
AutoSellBtn.Font = Enum.Font.GothamBold
AutoSellBtn.Parent = MainFrame
Instance.new("UICorner", AutoSellBtn).CornerRadius = UDim.new(0, 6)

local MinusSellBtn = Instance.new("TextButton")
MinusSellBtn.Size = UDim2.new(0, 20, 0, 28)
MinusSellBtn.Position = UDim2.new(1, -44, 0, 98)
MinusSellBtn.BackgroundColor3 = Color3.fromRGB(150, 50, 50)
MinusSellBtn.Text = "-"
MinusSellBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinusSellBtn.TextSize = 10
MinusSellBtn.Font = Enum.Font.GothamBold
MinusSellBtn.Parent = MainFrame
Instance.new("UICorner", MinusSellBtn).CornerRadius = UDim.new(0, 6)

local PlusSellBtn = Instance.new("TextButton")
PlusSellBtn.Size = UDim2.new(0, 20, 0, 28)
PlusSellBtn.Position = UDim2.new(1, -22, 0, 98)
PlusSellBtn.BackgroundColor3 = Color3.fromRGB(50, 150, 50)
PlusSellBtn.Text = "+"
PlusSellBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
PlusSellBtn.TextSize = 10
PlusSellBtn.Font = Enum.Font.GothamBold
PlusSellBtn.Parent = MainFrame
Instance.new("UICorner", PlusSellBtn).CornerRadius = UDim.new(0, 6)

-- Bảng Trạng Thái
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, -16, 0, 18)
StatusLabel.Position = UDim2.new(0, 8, 0, 136)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "⚡ Trạng thái: Đang tắt"
StatusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
StatusLabel.TextSize = 9
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.Parent = MainFrame

-- ================= UI NHẬP PHÚT VÀ GIÂY TỰ BÁN CÁ =================
local TimeInputFrame = Instance.new("Frame")
TimeInputFrame.Size = UDim2.new(0, 190, 0, 115)
TimeInputFrame.Position = UDim2.new(0.5, -95, 0.5, -57)
TimeInputFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TimeInputFrame.Visible = false
TimeInputFrame.Active = true
TimeInputFrame.Draggable = true
TimeInputFrame.Parent = ScreenGui
Instance.new("UICorner", TimeInputFrame).CornerRadius = UDim.new(0, 8)
local TimeInputStroke = Instance.new("UIStroke")
TimeInputStroke.Color = Color3.fromRGB(0, 255, 200)
TimeInputStroke.Thickness = 2
TimeInputStroke.Parent = TimeInputFrame

local TimeInputTitle = Instance.new("TextLabel")
TimeInputTitle.Size = UDim2.new(1, 0, 0, 22)
TimeInputTitle.BackgroundTransparency = 1
TimeInputTitle.Text = "⚙️ Cài đặt Thời Gian Auto Sell"
TimeInputTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
TimeInputTitle.TextSize = 10
TimeInputTitle.Font = Enum.Font.GothamBold
TimeInputTitle.Parent = TimeInputFrame

local TimeDisplayLabel = Instance.new("TextLabel")
TimeDisplayLabel.Size = UDim2.new(1, 0, 0, 22)
TimeDisplayLabel.Position = UDim2.new(0, 0, 0, 24)
TimeDisplayLabel.BackgroundTransparency = 1
TimeDisplayLabel.Text = "Thời gian: 05:00 (300s)"
TimeDisplayLabel.TextColor3 = Color3.fromRGB(100, 255, 200)
TimeDisplayLabel.TextSize = 10
TimeDisplayLabel.Font = Enum.Font.GothamBold
TimeDisplayLabel.Parent = TimeInputFrame

local function updateTimeDisplay()
    local m = math.floor(autoSellSeconds / 60)
    local s = autoSellSeconds % 60
    TimeDisplayLabel.Text = string.format("Thời gian: %02d:%02d (%ds)", m, s, autoSellSeconds)
end

local SubMinBtn = Instance.new("TextButton")
SubMinBtn.Size = UDim2.new(0, 35, 0, 20)
SubMinBtn.Position = UDim2.new(0.08, 0, 0, 50)
SubMinBtn.BackgroundColor3 = Color3.fromRGB(150, 50, 50)
SubMinBtn.Text = "-1m"
SubMinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SubMinBtn.TextSize = 9
SubMinBtn.Font = Enum.Font.GothamBold
SubMinBtn.Parent = TimeInputFrame
Instance.new("UICorner", SubMinBtn).CornerRadius = UDim.new(0, 5)

local AddMinBtn = Instance.new("TextButton")
AddMinBtn.Size = UDim2.new(0, 35, 0, 20)
AddMinBtn.Position = UDim2.new(0.38, 0, 0, 50)
AddMinBtn.BackgroundColor3 = Color3.fromRGB(50, 150, 50)
AddMinBtn.Text = "+1m"
AddMinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AddMinBtn.TextSize = 9
AddMinBtn.Font = Enum.Font.GothamBold
AddMinBtn.Parent = TimeInputFrame
Instance.new("UICorner", AddMinBtn).CornerRadius = UDim.new(0, 5)

local Sub10SecBtn = Instance.new("TextButton")
Sub10SecBtn.Size = UDim2.new(0, 35, 0, 20)
Sub10SecBtn.Position = UDim2.new(0.08, 0, 0, 75)
Sub10SecBtn.BackgroundColor3 = Color3.fromRGB(150, 80, 50)
Sub10SecBtn.Text = "-10s"
Sub10SecBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
Sub10SecBtn.TextSize = 9
Sub10SecBtn.Font = Enum.Font.GothamBold
Sub10SecBtn.Parent = TimeInputFrame
Instance.new("UICorner", Sub10SecBtn).CornerRadius = UDim.new(0, 5)

local Add10SecBtn = Instance.new("TextButton")
Add10SecBtn.Size = UDim2.new(0, 35, 0, 20)
Add10SecBtn.Position = UDim2.new(0.38, 0, 0, 75)
Add10SecBtn.BackgroundColor3 = Color3.fromRGB(50, 120, 150)
Add10SecBtn.Text = "+10s"
Add10SecBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
Add10SecBtn.TextSize = 9
Add10SecBtn.Font = Enum.Font.GothamBold
Add10SecBtn.Parent = TimeInputFrame
Instance.new("UICorner", Add10SecBtn).CornerRadius = UDim.new(0, 5)

local CloseTimeBtn = Instance.new("TextButton")
CloseTimeBtn.Size = UDim2.new(0, 45, 0, 45)
CloseTimeBtn.Position = UDim2.new(0.68, 0, 0, 50)
CloseTimeBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
CloseTimeBtn.Text = "OK"
CloseTimeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseTimeBtn.TextSize = 10
CloseTimeBtn.Font = Enum.Font.GothamBold
CloseTimeBtn.Parent = TimeInputFrame
Instance.new("UICorner", CloseTimeBtn).CornerRadius = UDim.new(0, 5)

PlusSellBtn.MouseButton1Click:Connect(function()
    TimeInputFrame.Visible = not TimeInputFrame.Visible
end)

MinusSellBtn.MouseButton1Click:Connect(function()
    if autoSellSeconds > 10 then
        autoSellSeconds = autoSellSeconds - 10
        updateTimeDisplay()
    end
end)

SubMinBtn.MouseButton1Click:Connect(function()
    if autoSellSeconds >= 60 then
        autoSellSeconds = autoSellSeconds - 60
    else
        autoSellSeconds = 0
    end
    updateTimeDisplay()
end)

AddMinBtn.MouseButton1Click:Connect(function()
    autoSellSeconds = autoSellSeconds + 60
    updateTimeDisplay()
end)

Sub10SecBtn.MouseButton1Click:Connect(function()
    if autoSellSeconds >= 10 then
        autoSellSeconds = autoSellSeconds - 10
    else
        autoSellSeconds = 0
    end
    updateTimeDisplay()
end)

Add10SecBtn.MouseButton1Click:Connect(function()
    autoSellSeconds = autoSellSeconds + 10
    updateTimeDisplay()
end)

CloseTimeBtn.MouseButton1Click:Connect(function()
    TimeInputFrame.Visible = false
end)

-- Logic Speed Buff
local isSpeedBuffEnabled = false
SpeedBuffBtn.MouseButton1Click:Connect(function()
    isSpeedBuffEnabled = not isSpeedBuffEnabled
    local lp = Players.LocalPlayer
    if isSpeedBuffEnabled then
        SpeedBuffBtn.Text = "⚡ Speed Buff: BẬT (40)"
        SpeedBuffBtn.BackgroundColor3 = Color3.fromRGB(40, 180, 80)
        if lp.Character and lp.Character:FindFirstChild("Humanoid") then
            lp.Character.Humanoid.WalkSpeed = 40
        end
    else
        SpeedBuffBtn.Text = "⚡ Speed Buff: TẮT (40)"
        SpeedBuffBtn.BackgroundColor3 = Color3.fromRGB(60, 100, 200)
        if lp.Character and lp.Character:FindFirstChild("Humanoid") then
            lp.Character.Humanoid.WalkSpeed = 16
        end
    end
end)

Players.LocalPlayer.CharacterAdded:Connect(function(char)
    char:WaitForChild("Humanoid")
    if isSpeedBuffEnabled then
        char.Humanoid.WalkSpeed = 40
    end
end)

-- Hàm lấy vị trí ngẫu nhiên theo hình chữ nhật cố định (X = 120, Y = 60)
local function getRandomScreenCenterPosition()
    local vpSize = workspace.CurrentCamera.ViewportSize
    local inset = GuiService:GetGuiInset()
    local randomX = (vpSize.X / 2) + 50 + math.random(-boxWidthSize, boxWidthSize)
    local randomY = (vpSize.Y / 2) - 50 + math.random(-boxHeightSize, boxHeightSize) + inset.Y
    return randomX, randomY
end

-- Hàm spam các phím Z, X, C, V cực nhanh
local function spamSkillKeys()
    local keys = {Enum.KeyCode.Z, Enum.KeyCode.X, Enum.KeyCode.C, Enum.KeyCode.V}
    for _, key in ipairs(keys) do
        VirtualInputManager:SendKeyEvent(true, key, false, game)
        VirtualInputManager:SendKeyEvent(false, key, false, game)
    end
end

local isRunning = false
local cameraLockConnection = nil

local function enableCameraLock()
    if cameraLockConnection then cameraLockConnection:Disconnect() end
    cameraLockConnection = RunService.RenderStepped:Connect(function()
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local rootPart = char.HumanoidRootPart
            local camPos = rootPart.Position - (rootPart.CFrame.LookVector * 6) + Vector3.new(0, 3, 0)
            Camera.CFrame = CFrame.new(camPos, rootPart.Position + Vector3.new(0, 1, 0))
        end
    end)
end

local function disableCameraLock()
    if cameraLockConnection then
        cameraLockConnection:Disconnect()
        cameraLockConnection = nil
    end
    Camera.CameraType = Enum.CameraType.Custom
end

local function startAutoMaster()
    if isRunning then return end
    isRunning = true
    ToggleMasterBtn.Text = "🛑 TẮT AUTO"
    ToggleMasterBtn.BackgroundColor3 = Color3.fromRGB(220, 50, 50)
    StatusLabel.Text = "🔥 Đang chạy Auto Farm Siêu Tốc..."
    
    enableCameraLock()
    
    task.spawn(function()
        while isRunning do
            -- 1. Giữ chuột siêu tốc tại vị trí ngẫu nhiên
            pcall(function()
                if not isRunning then return end
                local x, y = getRandomScreenCenterPosition()
                
                VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 0)
                
                local startTime = tick()
                while tick() - startTime < 0.2 and isRunning do -- Giảm thời gian giữ chuột xuống 0.2s để lặp cực nhanh
                    spamSkillKeys()
                    task.wait(0.01)
                end
                
                VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 0)
            end)
            
            if not isRunning then break end
            
            -- 2. Click liên tục siêu nhanh 15 cái với độ trễ tối thiểu
            pcall(function()
                for i = 1, 15 do
                    if not isRunning then break end
                    local x, y = getRandomScreenCenterPosition()
                    
                    VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 0)
                    spamSkillKeys()
                    VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 0)
                    
                    task.wait(0.01) -- Giảm độ trễ xuống mức thấp nhất để click cực nhanh
                end
            end)
            
            task.wait(0.01) -- Thời gian nghỉ giữa các vòng lặp cực ngắn
        end
    end)
end

local function stopAutoMaster()
    if not isRunning then return end
    isRunning = false
    ToggleMasterBtn.Text = "auto câu + skill"
    ToggleMasterBtn.BackgroundColor3 = Color3.fromRGB(40, 200, 90)
    StatusLabel.Text = "⚡ Trạng thái: Đang tắt"
    
    disableCameraLock()
    
    local lp = Players.LocalPlayer
    if lp.Character and lp.Character:FindFirstChild("Humanoid") then
        lp.Character.Humanoid.WalkSpeed = isSpeedBuffEnabled and 40 or 16
        lp.Character.Humanoid.JumpPower = 50
    end
end

ToggleMasterBtn.MouseButton1Click:Connect(function()
    if isRunning then
        stopAutoMaster()
    else
        startAutoMaster()
    end
end)

-- ================= HÀM BAY XUYÊN ĐẤT & TRỒI LÊN =================
local function flySmoothlyTo(targetCFrame)
    local lp = Players.LocalPlayer
    local char = lp.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") or not char:FindFirstChild("Humanoid") then return false end
    
    local hrp = char.HumanoidRootPart
    local humanoid = char.Humanoid
    
    humanoid.PlatformStand = true
    local speed = 70
    local runServiceConnection
    local noclipConnection
    
    noclipConnection = RunService.Stepped:Connect(function()
        if char then
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end)
    
    runServiceConnection = RunService.Stepped:Connect(function(_, dt)
        if not char or not char:FindFirstChild("HumanoidRootPart") then
            if runServiceConnection then runServiceConnection:Disconnect() end
            if noclipConnection then noclipConnection:Disconnect() end
            return
        end
        
        local currentPos = hrp.Position
        local undergroundTarget = targetCFrame.Position - Vector3.new(0, 25, 0)
        local direction = (undergroundTarget - currentPos)
        local distance = direction.Magnitude
        local surfaceDistance = (Vector3.new(currentPos.X, targetCFrame.Position.Y, currentPos.Z) - targetCFrame.Position).Magnitude
        
        if surfaceDistance < 6 then
            hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
            if runServiceConnection then runServiceConnection:Disconnect() end
        else
            local velocity = direction.Unit * math.min(speed, distance * 10)
            hrp.AssemblyLinearVelocity = velocity
            hrp.CFrame = CFrame.new(currentPos, undergroundTarget)
        end
    end)
    
    local timeout = tick() + 15
    while (Vector3.new(hrp.Position.X, targetCFrame.Position.Y, hrp.Position.Z) - targetCFrame.Position).Magnitude > 6 and tick() < timeout and char:FindFirstChild("HumanoidRootPart") do
        task.wait(0.05)
    end
    
    if runServiceConnection then runServiceConnection:Disconnect() end
    if noclipConnection then noclipConnection:Disconnect() end
    
    if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") then
        hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
        local surfacePos = targetCFrame + Vector3.new(0, 6, 0)
        hrp.CFrame = surfacePos
        task.wait(0.15)
        
        for _, part in ipairs(char:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = true
            end
        end
        
        humanoid.PlatformStand = false
        hrp.CFrame = targetCFrame
    end
    
    local finalDist = (hrp.Position - targetCFrame.Position).Magnitude
    return finalDist <= 12
end

-- ================= LOGIC AUTO SELL =================
local isAutoSellRunning = false

AutoSellBtn.MouseButton1Click:Connect(function()
    isAutoSellRunning = not isAutoSellRunning
    
    if isAutoSellRunning then
        AutoSellBtn.Text = "📦 Auto Sell: BẬT"
        AutoSellBtn.BackgroundColor3 = Color3.fromRGB(200, 140, 40)
        
        task.spawn(function()
            while isAutoSellRunning do
                local totalSeconds = autoSellSeconds
                while totalSeconds > 0 and isAutoSellRunning do
                    local mins = math.floor(totalSeconds / 60)
                    local secs = totalSeconds % 60
                    StatusLabel.Text = string.format("⏳ Auto Sell: %02d:%02d", mins, secs)
                    task.wait(1)
                    totalSeconds = totalSeconds - 1
                end
                
                if not isAutoSellRunning then break end
                
                StatusLabel.Text = "🖱️ Nhấn giữa màn hình 3 lần..."
                pcall(function()
                    local vpSize = workspace.CurrentCamera.ViewportSize
                    local midX = vpSize.X / 2
                    local midY = vpSize.Y / 2
                    local insetY = GuiService:GetGuiInset().Y
                    
                    for i = 1, 3 do
                        if not isAutoSellRunning then break end
                        VirtualInputManager:SendMouseButtonEvent(midX, midY + insetY, 0, true, game, 0)
                        task.wait(0.02)
                        VirtualInputManager:SendMouseButtonEvent(midX, midY + insetY, 0, false, game, 0)
                        task.wait(0.1)
                    end
                end)
                task.wait(0.3)

                StatusLabel.Text = "🛑 Tạm dừng Auto đi bán cá..."
                if isRunning then
                    stopAutoMaster()
                end
                
                task.wait(0.3)
                local lp = Players.LocalPlayer
                local char = lp.Character
                if char and char:FindFirstChild("HumanoidRootPart") then
                    local originalCFrame = char.HumanoidRootPart.CFrame
                    StatusLabel.Text = "📍 Đã lưu vị trí câu."
                    
                    StatusLabel.Text = "🧭 Tìm NPC bán cá..."
                    local targetNpcPart = nil
                    local shortestDist = math.huge
                    
                    for _, obj in ipairs(workspace:GetDescendants()) do
                        local nameLower = string.lower(obj.Name)
                        if nameLower == "npc_fish_seller_1" or string.find(nameLower, "seller") or string.find(nameLower, "fish_seller") then
                            local part = nil
                            if obj:IsA("Model") then
                                part = obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart")
                            elseif obj:IsA("BasePart") then
                                part = obj
                            end
                            
                            if part then
                                local dist = (char.HumanoidRootPart.Position - part.Position).Magnitude
                                if dist < shortestDist then
                                    shortestDist = dist
                                    targetNpcPart = part
                                end
                            end
                        end
                    end
                    
                    if targetNpcPart then
                        StatusLabel.Text = "🕳️ Đang bay tới NPC..."
                        local targetPos = targetNpcPart.Position + Vector3.new(0, 3, 5)
                        
                        local arrivedAtNpc = flySmoothlyTo(CFrame.new(targetPos))
                        
                        if arrivedAtNpc then
                            StatusLabel.Text = "✅ Đã tới vị trí bán cá!"
                            task.wait(0.3)
                            
                            pcall(function()
                                for _, obj in ipairs(workspace:GetDescendants()) do
                                    local nameLower = string.lower(obj.Name)
                                    if nameLower == "npc_fish_seller_1" or string.find(nameLower, "seller") then
                                        local part = nil
                                        if obj:IsA("Model") then
                                            part = obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart")
                                        elseif obj:IsA("BasePart") then
                                            part = obj
                                        end
                                        
                                        if part and (part.Position - char.HumanoidRootPart.Position).Magnitude < 30 then
                                            for _, prompt in ipairs(part:GetDescendants()) do
                                                if prompt:IsA("ProximityPrompt") then
                                                    fireproximityprompt(prompt)
                                                end
                                            end
                                        end
                                    end
                                end
                            end)
                            
                            StatusLabel.Text = "⏳ Đợi hội thoại..."
                            task.wait(1.0)
                            
                            local function clickButtonByName(btnName)
                                local clicked = false
                                pcall(function()
                                    for _, gui in ipairs(PlayerGui:GetChildren()) do
                                        if gui:IsA("ScreenGui") then
                                            for _, desc in ipairs(gui:GetDescendants()) do
                                                if desc.Name == btnName and desc:IsA("TextButton") and desc.Visible then
                                                    local absPos = desc.AbsolutePosition
                                                    local absSize = desc.AbsoluteSize
                                                    local inset = GuiService:GetGuiInset()
                                                    local finalX = absPos.X + (absSize.X / 2)
                                                    local finalY = absPos.Y + (absSize.Y / 2) + inset.Y
                                                    
                                                    VirtualInputManager:SendMouseButtonEvent(finalX, finalY, 0, true, game, 0)
                                                    task.wait(0.02)
                                                    VirtualInputManager:SendMouseButtonEvent(finalX, finalY, 0, false, game, 0)
                                                    clicked = true
                                                end
                                            end
                                        end
                                    end
                                end)
                                return clicked
                            end
                            
                            StatusLabel.Text = "🛒 Nhấn Option002 (1)..."
                            clickButtonByName("Option002")
                            task.wait(1.0)
                            
                            StatusLabel.Text = "🛒 Nhấn Option001..."
                            clickButtonByName("Option001")
                            task.wait(1.0)
                            
                            StatusLabel.Text = "🛒 Nhấn Option002 (2)..."
                            clickButtonByName("Option002")
                            task.wait(0.5)
                        else
                            StatusLabel.Text = "⚠️ Không thể tới NPC!"
                            task.wait(1)
                        end
                        
                        StatusLabel.Text = "🕳️ Đang bay về vị trí cũ..."
                        local arrivedAtOldPos = flySmoothlyTo(originalCFrame)
                        
                        if arrivedAtOldPos then
                            StatusLabel.Text = "✅ Đã về lại vị trí cũ!"
                        else
                            if char and char:FindFirstChild("HumanoidRootPart") then
                                char.HumanoidRootPart.CFrame = originalCFrame
                            end
                            StatusLabel.Text = "✨ Đã khôi phục vị trí."
                        end
                        
                        task.wait(0.3)
                        
                    else
                        StatusLabel.Text = "⚠️ Không thấy NPC!"
                        task.wait(1)
                    end
                end
                
                if isAutoSellRunning then
                    StatusLabel.Text = "🚀 Bật lại auto câu..."
                    task.wait(0.5)
                    if isAutoSellRunning then
                        startAutoMaster()
                    end
                end
                
                task.wait(0.5)
            end
        end)
    else
        AutoSellBtn.Text = "📦 Auto Sell: TẮT"
        AutoSellBtn.BackgroundColor3 = Color3.fromRGB(180, 100, 40)
        StatusLabel.Text = "⚡ Trạng thái: Đang tắt"
    end
end)
